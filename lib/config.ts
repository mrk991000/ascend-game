// ─── EDIT THESE THREE LINES ───────────────────────────────────────────────
export const GITHUB_USERNAME = "mrk991000";
export const GITHUB_REPO = "ascend-game";
export const APK_FILENAME = "ascend.apk";
// ────────────────────────────────────────────────────────────────────────

// This pattern always points at whatever asset with this exact filename
// is attached to your most recent GitHub Release — no need to update the
// link every time you cut a new release, as long as the filename matches.
export const DOWNLOAD_URL = `https://github.com/${GITHUB_USERNAME}/${GITHUB_REPO}/releases/latest/download/${APK_FILENAME}`;
export const REPO_URL = `https://github.com/${GITHUB_USERNAME}/${GITHUB_REPO}`;
export const RELEASES_URL = `https://github.com/${GITHUB_USERNAME}/${GITHUB_REPO}/releases`;
